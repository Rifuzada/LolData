{
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public dist && cp -r views dist"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^8.6.0",
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}