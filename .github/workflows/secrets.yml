on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3

            - name: Criar arquivo .env
              run: |
                  echo "API_KEY=${{ secrets.API_KEY }}" > .env
